<% @page_title = "Menu" %>

<div class="menu">
  <h2>Menu</h2>
  <div class="identity">Logged in as: <%= session[:username]  %>
  <% if session[:level] > 8  %>
    , Administrator
  <% end  %>
  </div>
  <ul>
    <li><%= link_to("Manage Media Files", :controller => 'mediafiles') %></li>
    <li><%= link_to("Manage Media", :controller => 'media') %></li>
    <% if session[:level] > 8  %>
    <li><%= link_to("Manage Users", :controller => 'users') %></li>
    <% end  %>
    <li><%= link_to("Logout", :action => 'logout') %></li>
  </ul>
</div>
