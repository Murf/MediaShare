<h3>All Media</h3>
<div class="span8">
  <table class="table table-condensed">
    <thead>
    <tr>
      <th>Title</th>
      <th>Type</th>
      <th>Genre</th>
      <th>Owner</th>
      <th>Imdb ID</th>
      <th></th>
    </tr>
    </thead>
    <tbody>
    <% @media.each do |medium| %>
        <tr>
          <td><%= link_to medium.title, medium %></td>
          <td><%= medium.type %></td>
          <td><%= medium.genre %></td>
          <%     @owner="Me"
                 if (medium.mediafiles.count == 1 && medium.mediafiles.first.user_id != current_user.id)
                   @owner = medium.mediafiles.first.user.fullname
                 elsif (medium.mediafiles.count > 1)
                   @owner = "Multiple"
                 end
          %>
          <td><%= @owner %></td>
          <td><%= medium.imdb_id %></td>
          <td><%= link_to("Delete", medium_path(medium), :method => :delete, :class => 'btn btn-mini')  %></td>
        </tr>
    <% end %>
    </tbody>
  </table>
</div>

